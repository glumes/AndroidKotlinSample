<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.glumes.androidkotlinsample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SampleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>
        <activity android:name=".anko.AnkoActivity" />
        <activity android:name=".anko.AnkoFragmentActivity" />
        <activity android:name=".anko.RefreshListActivity" />

        <activity
            android:name=".gankio.GankIOActivity"
            android:screenOrientation="portrait"
            android:theme="@style/TransparentTheme">

        </activity>


        <activity android:name=".gankio.ArticleActivity" />

        <activity android:name=".gankio.OpenEyeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".gankio.VideoActivity" />
        <activity android:name=".gankio.MeiZiActivity"></activity>
    </application>

</manifest>